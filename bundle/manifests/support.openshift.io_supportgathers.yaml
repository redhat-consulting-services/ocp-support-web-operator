---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: supportgathers.support.openshift.io
spec:
  group: support.openshift.io
  names:
    kind: SupportGather
    listKind: SupportGatherList
    plural: supportgathers
    singular: supportgather
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.upload.caseID
      name: Case
      priority: 1
      type: string
    - jsonPath: .status.progress
      name: Progress
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              anonymize:
                description: Anonymize enables data anonymization in the gathered
                  archive.
                type: boolean
              gatherTypes:
                description: |-
                  GatherTypes specifies which must-gather types to run.
                  Use ["all"] to run all detected operators, or specify individual types
                  like ["virtualization", "odf", "acm"]. Default must-gather always runs.
                items:
                  type: string
                type: array
              includeLogs:
                description: IncludeLogs controls whether pod logs are collected in
                  custom gather mode.
                type: boolean
              namespaces:
                description: |-
                  Namespaces limits the gather to specific namespaces (custom gather mode).
                  If set, only these namespaces are collected instead of the full cluster gather.
                items:
                  type: string
                type: array
              resourceTypes:
                description: |-
                  ResourceTypes specifies which resource types to collect in custom gather mode.
                  Only used when Namespaces is set.
                items:
                  type: string
                type: array
              since:
                description: Since limits log collection to this time window (e.g.
                  "6h", "24h", "48h").
                type: string
              upload:
                description: Upload configures automatic upload to Red Hat support
                  after gather completes.
                properties:
                  caseID:
                    description: CaseID is the Red Hat support case number to attach
                      the archive to.
                    type: string
                  internalUser:
                    description: |-
                      InternalUser routes uploads to the internal Red Hat path (/case-mgmt/)
                      instead of the external path (/incoming/).
                    type: boolean
                  secretRef:
                    description: |-
                      SecretRef references a Secret containing "username" and "password" keys
                      for Red Hat's SFTP server (sftp.access.redhat.com).
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - caseID
                - secretRef
                type: object
            type: object
          status:
            properties:
              completionTime:
                description: CompletionTime is when the gather (and upload) finished.
                format: date-time
                type: string
              conditions:
                description: Conditions provide detailed status information.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              error:
                description: Error contains the error message if the gather or upload
                  failed.
                type: string
              fileName:
                description: FileName is the name of the generated archive.
                type: string
              jobID:
                description: JobID is the backend gather job ID.
                type: string
              phase:
                description: 'Phase is the current state: Pending, Gathering, Uploading,
                  Complete, Failed.'
                type: string
              progress:
                description: Progress is the gather completion percentage (0-100).
                type: integer
              uploadStatus:
                description: UploadStatus reports the upload result if upload was
                  configured.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
