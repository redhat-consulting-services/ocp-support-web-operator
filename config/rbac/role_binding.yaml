apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: ocp-support-web-operator-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: ocp-support-web-operator-role
subjects:
  - kind: ServiceAccount
    name: ocp-support-web-operator
    namespace: system
